name: Build and Publish Docker Images

on:
  push:
    branches:
      - review-3 

permissions:
    packages: write
    contents: read
  
jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:

      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

      - name: Build and Push Backend
        run: |
          docker build -t ghcr.io/shinqwe/film-react-nest/backend:latest ./backend
          docker push ghcr.io/shinqwe/film-react-nest/backend:latest

      - name: Build and Push Frontend
        run: |
          docker build -t ghcr.io/shinqwe/film-react-nest/frontend:latest ./frontend
          docker push ghcr.io/shinqwe/film-react-nest/frontend:latest 